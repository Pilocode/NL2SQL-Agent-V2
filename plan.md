# NL2SQL Agent 项目设计方案

## 1. 项目定位

本项目定位为“课程大作业级别的可运行 NL2SQL Agent 系统”，目标不是追求论文 SOTA，而是做出一个：

- 可以稳定跑通的完整系统
- 有明确技术设计，而不是纯 API 套壳
- 有清晰模块边界和架构思考
- 便于课堂演示和答辩展示

项目建议命名：NL2SQL Agent。

一句话定义：

一个能够接收自然语言问题，自动选择相关 schema，上下文检索、生成 SQL、执行校验并返回结果解释的轻量级智能数据查询系统。

## 2. 课程评分导向拆解

### 2.1 功能完整性

老师最先会看系统能不能跑通，因此第一优先级是形成完整闭环：

1. 用户输入自然语言问题
2. 系统检索相关表、字段、业务术语
3. 系统生成候选 SQL
4. 系统校验 SQL 合法性和安全性
5. 系统执行 SQL 并返回结果
6. 系统展示 SQL、结果表和自然语言解释

最低交付标准：

- 至少支持一个真实数据库
- 至少支持 8 到 10 个可演示问题
- 至少支持错误恢复一次，例如首次 SQL 出错后自动修正

### 2.2 技术深度

为了避免被评价为“只是调了个大模型 API”，项目必须加入至少 3 个非纯调用式模块。

建议选择下面四项中的三到四项：

1. 自定义 schema 检索模块
2. 轻量语义层或术语映射模块
3. SQL AST 校验与安全约束模块
4. 执行反馈驱动的自动修正模块

这四项都不依赖重新训练模型，但都体现了真正的系统设计和算法/工程实现能力。

### 2.3 系统设计

系统设计评分通常看三点：

1. 模块是否清晰
2. 数据流是否合理
3. 是否考虑扩展性与稳定性

因此本项目不建议做成单文件脚本，而应做成分层结构。

### 2.4 展示效果

课堂演示不怕功能少，怕不稳定。因此展示策略要优先追求：

- 本地可跑
- 数据固定
- 问题固定
- 输出稳定
- 页面清晰

项目演示时最好不要依赖复杂外部服务链路。

## 3. 项目目标与边界

### 3.1 项目目标

- 构建一个面向分析型查询场景的 NL2SQL Agent 原型
- 支持中文自然语言输入
- 支持数据库 schema 检索与上下文裁剪
- 支持 SQL 生成、校验、执行、解释
- 支持简单的多轮上下文追问
- 提供网页界面用于演示

### 3.2 非目标

为了保证课程项目可完成，以下内容不作为第一版目标：

- 不追求自训练 Text-to-SQL 大模型
- 不做复杂分布式部署
- 不做企业级权限中心
- 不做跨多个异构数据库联邦查询
- 不做完整 BI 平台

## 4. 建议的最终交付形态

建议最终交付包含以下内容：

1. 一个可启动的 Web 应用
2. 一个可查询的示例数据库
3. 一套固定的演示问题
4. 一份项目报告
5. 一份系统架构图
6. 一段答辩演示流程

## 5. 推荐系统方案

### 5.1 方案总览

推荐做一个“混合式 NL2SQL Agent”：

- 大模型负责理解和生成
- 自定义检索模块负责缩小 schema 范围
- 规则/解析模块负责约束输出质量
- SQL 执行模块负责结果闭环

这比“输入问题 -> 直接让模型吐 SQL”更有技术深度，也更稳定。

### 5.2 核心设计思想

本项目的关键不是追求最强模型，而是建立以下链路：

自然语言问题 -> schema 检索 -> 上下文构造 -> SQL 生成 -> SQL 校验 -> 执行反馈 -> 最终答案

这条链路本身就是项目的主要技术价值。

## 6. 系统架构设计

### 6.1 架构分层

建议采用四层架构。

#### 表现层

- Web UI
- 输入问题
- 展示候选表、生成 SQL、查询结果、解释文本

推荐技术：Streamlit 或前后端分离方案。

课程项目优先建议：Streamlit。

原因：

- 开发快
- 演示稳定
- 表格展示方便
- 对 Python 后端整合简单

#### 应用编排层

- Agent Orchestrator
- 控制查询流程
- 串联检索、生成、校验、执行、解释

这一层建议自己实现，不要完全依赖现成 Agent 框架黑盒。

#### 核心能力层

- Schema Retriever
- Semantic Mapper
- SQL Generator
- SQL Validator
- SQL Repairer
- Result Explainer

#### 数据层

- SQLite / DuckDB 示例数据库
- schema metadata
- 术语映射表
- few-shot 示例库

### 6.2 模块级数据流

#### 模块 1：Query Analyzer

输入：用户自然语言问题

输出：

- 问题关键词
- 可能涉及的实体词
- 是否是追问

实现建议：

- 规则提取 + LLM 辅助理解
- 对追问问题拼接历史上下文

#### 模块 2：Schema Retriever

输入：用户问题

输出：候选表、候选字段、候选关联关系

这是项目里最重要的“不是纯 API”的模块之一。

建议实现：

- 从数据库读取 schema metadata
- 构建表名、字段名、注释、别名、业务术语索引
- 采用混合检索：
  - 关键词匹配
  - 简单相似度匹配
  - 可选向量检索

最简可行实现：

- BM25/字符串相似度 + 人工维护 synonym 表

增强实现：

- embedding 检索候选字段说明

#### 模块 3：Semantic Mapper

输入：用户问题 + 候选 schema

输出：业务术语到数据库字段的映射结果

示例：

- 销售额 -> total_amount
- 用户数 -> count(distinct user_id)
- 本月 -> 当前月份过滤条件

这个模块建议做成轻量语义层，不需要很复杂，但能显著提升答辩说服力。

可以用一个 YAML 或 JSON 文件维护：

- 指标名
- 维度名
- 同义词
- 默认聚合方式
- 时间字段

#### 模块 4：Prompt Builder

输入：

- 用户问题
- 候选 schema
- 术语映射
- few-shot 示例

输出：面向 LLM 的最终 prompt

重点：

- 只放相关表和字段
- 提供 join hint
- 指定 SQL 方言
- 明确只允许只读 SQL
- 要求返回结构化输出

#### 模块 5：SQL Generator

输入：构造后的 prompt

输出：候选 SQL

建议采用“分解式生成”而不是一次直出，例如：

1. 先总结查询意图
2. 再确定涉及表和字段
3. 再生成 SQL

这会比直接问模型更稳定，也更有技术味道。

#### 模块 6：SQL Validator

输入：候选 SQL

输出：校验结果

建议校验内容：

- 是否能被 SQL parser 正确解析
- 是否包含非只读操作
- 是否引用不存在的表或字段
- 是否缺少 LIMIT 或存在高风险全表扫描

建议技术：sqlglot。

这也是核心技术深度来源之一，因为它体现了形式语言约束与系统安全意识。

#### 模块 7：SQL Repairer

输入：校验失败原因或执行错误

输出：修复后的 SQL

示例：

- 表名不存在 -> 重新检索 schema 后修复
- 字段不存在 -> 回退到 Prompt Builder 重构上下文
- SQL 语法错误 -> 让模型带错误信息重新生成

这会让演示明显更稳定，也能体现 Agent 的闭环能力。

#### 模块 8：Executor + Explainer

输入：最终 SQL

输出：

- 查询结果表
- 自然语言解释
- 可选图表

展示时建议同时显示：

- 识别到的候选表
- 最终 SQL
- 查询结果
- 一段解释文本

这样老师能直观看到系统不是黑盒。

## 7. 体现“不是纯调用 API”的关键技术点

本项目建议明确宣传以下 4 个技术点。

### 7.1 自定义 Schema 检索

不是把全量 schema 直接喂给模型，而是先做相关表字段召回和裁剪。

可展示的实现内容：

- metadata 抽取脚本
- schema index 构建
- 召回排序逻辑

### 7.2 轻量语义层

不是只看数据库技术字段名，而是建立业务词和字段之间的映射。

可展示的实现内容：

- metric/dimension 配置文件
- 同义词表
- 时间语义映射

### 7.3 SQL AST 校验

不是生成 SQL 后直接执行，而是先做语法和安全检查。

可展示的实现内容：

- sqlglot parse
- 只读检查
- 风险关键字拦截
- 自动补 LIMIT 策略

### 7.4 执行反馈修正闭环

不是只生成一次失败就结束，而是能基于错误消息自动修正。

可展示的实现内容：

- 重试机制
- 错误分类
- 修正前后 SQL 对比

## 8. 技术选型建议

### 8.1 后端

- Python 3.11
- FastAPI 或纯 Streamlit

课程项目建议：

- 如果时间有限，用 Streamlit + Python 模块化后端
- 如果想提高系统设计分，用 FastAPI + Streamlit 前端

### 8.2 数据库

- SQLite 或 DuckDB

课程项目优先建议：SQLite。

原因：

- 零部署
- 演示稳定
- 易于打包提交

### 8.3 大模型接入

- 可选 OpenAI 兼容接口
- 可选 Qwen / DeepSeek / 通义 / OpenAI 任一 API

重点不是绑死某个模型，而是把模型封装为统一接口。

### 8.4 SQL 解析与校验

- sqlglot

### 8.5 检索组件

- 第一版：基于 JSON metadata 的关键词匹配 + 相似度排序
- 第二版：可选 FAISS / Chroma 向量检索

### 8.6 前端展示

- Streamlit

展示上建议包含以下区域：

- 问题输入框
- 候选 schema 展示
- 生成 SQL 展示
- 结果表展示
- 图表展示
- 调试日志折叠面板

## 9. 推荐项目目录结构

建议目录结构如下：

```text
NL2SQL-Agent/
├─ app/
│  ├─ main.py
│  ├─ ui.py
│  ├─ orchestrator.py
│  ├─ config.py
│  └─ utils/
├─ core/
│  ├─ query_analyzer.py
│  ├─ schema_retriever.py
│  ├─ semantic_mapper.py
│  ├─ prompt_builder.py
│  ├─ sql_generator.py
│  ├─ sql_validator.py
│  ├─ sql_repairer.py
│  └─ result_explainer.py
├─ data/
│  ├─ demo.db
│  ├─ schema_metadata.json
│  ├─ semantic_layer.yaml
│  └─ fewshot_examples.json
├─ tests/
│  ├─ test_retriever.py
│  ├─ test_validator.py
│  └─ test_e2e_cases.py
├─ docs/
│  └─ architecture.png
├─ search_result.md
├─ plan.md
└─ PROGRESS.md
```

## 10. 建议的功能清单

### 10.1 必做功能

- 自然语言输入
- schema 检索
- SQL 生成
- SQL 校验
- SQL 执行
- 结果展示
- 错误自动修正一次

### 10.2 加分功能

- 多轮追问
- 图表自动生成
- 术语层配置
- 候选 SQL 对比
- 查询历史记录
- 评测模式

## 11. 演示稳定性设计

展示效果是课程项目成败关键之一，建议专门设计演示策略。

### 11.1 演示数据库策略

推荐使用本地内置 SQLite 数据库，不依赖远程数据库。

建议选一个结构适中、能体现 join/聚合/排序/时间过滤的数据库，例如：

- 电商订单库
- 教务选课库
- 音乐商店 Chinook

其中电商或教务更适合中文展示。

### 11.2 演示问题策略

提前准备 8 到 10 个问题，覆盖：

- 单表查询
- join 查询
- 聚合统计
- 排序 top-k
- 时间过滤
- 分组统计
- 多轮追问
- 一次错误修正案例

### 11.3 稳定性策略

- 使用固定数据库
- 使用固定 few-shot 示例
- 限制生成温度
- 所有查询都加只读和超时约束
- 关键演示问题预先回归测试

### 11.4 Demo 时的页面输出建议

每次查询显示四段：

1. 问题理解
2. 命中的 schema
3. 最终 SQL
4. 执行结果与解释

这样即使结果不完全正确，也能体现你的系统设计过程。

## 12. 评测与验收方案

### 12.1 离线评测

建立一个小型自定义测试集，例如 15 到 20 个问题。

每个问题保存：

- 自然语言问题
- 参考 SQL
- 期望结果说明

### 12.2 评测指标

课程项目不一定要上复杂 benchmark，但建议至少统计：

- SQL 可执行率
- 结果正确率
- 首次成功率
- 修正后成功率
- 平均响应时间

### 12.3 验收标准

建议答辩时宣称以下项目目标：

- 80% 以上问题可生成可执行 SQL
- 固定演示集成功率高于 80%
- 至少 1 个错误案例可以自动修正
- 所有执行均为只读、安全约束下完成

## 13. 开发阶段规划

### 第一阶段：最小闭环

- 建库
- 读取 schema
- 调用模型生成 SQL
- 执行并展示结果

目标：先跑通。

### 第二阶段：技术增强

- 加 schema 检索
- 加语义层
- 加 SQL 校验
- 加自动修正

目标：体现技术深度。

### 第三阶段：展示与包装

- 优化页面
- 固化演示问题
- 加日志展示
- 准备架构图和答辩材料

目标：稳定演示。

## 14. 答辩时可强调的创新点

如果老师问“你的创新点是什么”，建议回答不要说模型本身，而说系统方法：

1. 不是直接全 schema prompt，而是做了 schema 检索与裁剪
2. 引入了轻量语义层，把业务术语映射到字段和指标
3. 引入 SQL AST 校验和安全约束，避免盲目执行模型输出
4. 引入执行反馈修正闭环，提高系统稳定性
5. 把系统做成了可观测、可解释的 Agent 流程，而不是黑盒生成器

## 15. 最终建议

对课程项目来说，最优策略不是“做最大”，而是“做完整、做稳定、做得有设计感”。

因此本项目最推荐的版本是：

- 一个本地可跑的 SQLite + Streamlit 系统
- 一个带 schema 检索和轻量语义层的 NL2SQL Agent
- 一个带 SQL 校验与修正闭环的执行链路
- 一套固定可复现的演示问题

这个组合在“能跑、够深、有架构、好展示”四个维度上最均衡。

## 16. 一句话版本结论

本项目不应被做成“调用大模型生成 SQL 的 demo”，而应被做成“一个带检索、约束、修正和展示闭环的轻量智能查询系统”。